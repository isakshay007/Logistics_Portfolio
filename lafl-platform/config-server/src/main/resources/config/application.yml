jwt:
  secret: ${JWT_SECRET}
kafka:
  bootstrap-servers: ${KAFKA_BROKERS:localhost:9092}
redis:
  host: ${REDIS_HOST:localhost}
  port: ${REDIS_PORT:6379}
spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USER}
    password: ${DB_PASS}

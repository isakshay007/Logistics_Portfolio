jwt:
  secret: ${JWT_SECRET}
kafka:
  bootstrap-servers: ${KAFKA_BROKERS}
redis:
  host: ${REDIS_HOST}
  port: ${REDIS_PORT}
spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USER}
    password: ${DB_PASS}

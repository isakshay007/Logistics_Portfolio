applications:
  - name: config-server
    path: build/libs/config-server-0.1.0-SNAPSHOT.jar
    memory: 512M
    instances: 1
    no-route: true
    buildpacks:
      - java_buildpack
    env:
      CF_APP_NAME: config-server
      SPRING_PROFILES_ACTIVE: native,cloud
      SPRING_CLOUD_CONFIG_SERVER_NATIVE_SEARCH_LOCATIONS: classpath:/config
      SPRING_CLOUD_CONFIG_SERVER_GIT_URI: ""
      JAVA_OPTS: "-XX:ReservedCodeCacheSize=32M -XX:MaxDirectMemorySize=10M -Xss256k -XX:MaxMetaspaceSize=96M -XX:+UseSerialGC"
      JBP_CONFIG_OPEN_JDK_JRE: '{ jre: { version: 17.+ } }'
      JBP_CONFIG_JVM_KILL: 'enabled: false'

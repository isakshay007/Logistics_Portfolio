applications:
  - name: notification-service
    path: build/libs/notification-service-0.1.0-SNAPSHOT.jar
    memory: 512M
    instances: 1
    no-route: true
    buildpacks:
      - java_buildpack
    env:
      CF_APP_NAME: notification-service
      SPRING_PROFILES_ACTIVE: cloud
      JAVA_OPTS: "-XX:ReservedCodeCacheSize=32M -XX:MaxDirectMemorySize=10M -Xss256k -XX:MaxMetaspaceSize=96M -XX:+UseSerialGC"
      JBP_CONFIG_OPEN_JDK_JRE: '{ jre: { version: 17.+ } }'
      JBP_CONFIG_JVM_KILL: 'enabled: false'
